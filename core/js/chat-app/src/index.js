import React from 'react'
import ReactDOM from 'react-dom'
import './styles.css'
import App from './App'

const root = document.getElementById('aichwp-chat-app')
ReactDOM.createRoot(root).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
